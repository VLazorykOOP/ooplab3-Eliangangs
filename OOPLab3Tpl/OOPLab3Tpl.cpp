#include <iostream>
#include "Lab3.h"
using namespace std;

int main()
{
    cout << " Lab #3 (14v)  !\n";

//  Код виконання завдань
//  Головне меню завдань
//  Функції та класи можуть знаходитись в інших файлах проекту

    int conv = 1;
    if (conv == 1) conv = main1();
    if (conv == 1) conv = main2(); 
    if (conv == 1) conv = main3();
}

